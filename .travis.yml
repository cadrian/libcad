language: c
compiler:
  - gcc
before_install:
  - "sudo apt-get update -qq"
# - "sudo apt-get install -qq doxygen dot"
install:
  - "make lib"
  - "ls -lR target"
script:
  - "make run-test"
  - "make debuild"
  - "sudo dpkg -i target/dpkg/*.deb"
cache:
  apt: true
