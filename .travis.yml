language: c
compiler:
  - gcc
before_install:
  - "sudo apt-get update -qq"
install:
  - "make lib"
script:
  - "ls -lR target"
  - "make run-test"
  - "make doc"
  - "sudo dpkg -i target/dpkg/*.deb"
cache:
  apt: true
